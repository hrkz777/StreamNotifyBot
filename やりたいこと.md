# 配信通知BOT

## 概要
- 配信プラットフォームによるWebhook通知とAPIのポーリングを用いてデータを取得する
- DiscordへWebhook経由で通知を行う

## 環境
- PHP 8.5.9で動作する (無料のレンタルサーバーなどでもホストできるように)
- データベースはMariaDB 10.5を使用する
- Dockerfileを作成し、Dockerとして扱えるようにする
- 対応プラットフォーム一覧はコードで固定し、追加時はプラットフォーム固有処理を実装してデプロイする
- 定期処理と再試行はレンタルサーバーの Cron から PHP CLI コマンドを起動して実行し、専用キューや常駐ワーカーを必須としない

## 動作
- 新規追加時とYouTubeのように期限があるなら定期実行で、プラットフォームサーバーへWebhookを登録
- プラットフォームからの対象の新規登録を行う
- Webhookは変更を迅速に検知するために使用し、通知に使用する最新情報はAPIから取得する
- 配信情報はポーリングで更新し、Webhookの欠落、遅延、順序逆転を補完する
- Webhookとポーリングで同じ変更を取得しても、通知を重複させない
- YouTubeの初回登録時と障害復旧時はチャンネルのAtomフィードを取得し、動画IDを重複排除して50件ごとに`videos.list`から詳細を取得する
- TwitchとTwitCastingの初回登録時と障害復旧時は、現在配信中のライブと現在から30日以内の配信予定を取得し、過去の配信・録画履歴は遡及取得しない
- 初回・復旧同期はWebhook購読を先に開始し、外部IDによる重複排除、範囲を限定したページ取得、途中経過の保存によってCronの次回実行から再開できるようにする
- 障害中に開始・終了した配信を推測して遡及通知せず、復旧時点で確認できる最新状態だけを反映する
- 動画投稿・配信開始前・配信中・配信終了を通知する
- 配信開始前の通知には、配信予約・予約情報変更を含める
- 配信終了後も終了起点から正確に168時間未満は低頻度でポーリングを行う
- 終了起点から168時間に到達した時点でポーリングを停止し、通知投稿を削除対象にする
- 168時間の計算と保存はUTCで行い、管理画面でのみ利用者のタイムゾーンへ変換する
- 終了起点は、プラットフォームAPIの正式な終了時刻、検証済みWebhookのイベント発生時刻、ポーリングで終了を初めて確認した時刻の順に優先する
- 期限前により信頼度の高い終了時刻を取得した場合は期限を再計算し、配信中へ戻った場合は終了起点と期限を解除して追跡を再開する
- テーブルごとに、CSVによるエクスポート・インポートが行えるようにする
- 通常CSVは設定データを対象とし、UTF-8 BOM付き、カンマ区切り、CRLF、必須ヘッダー、スキーマバージョン付きの形式とする
- インポートは追加、追加・更新、置換を明示的に選択し、検証プレビューと管理者の確定後に1ファイル単位で全件反映または全件ロールバックする
- 秘密情報、実行履歴、YouTube API由来の外部IDや表示情報は通常CSVへ含めず、プラットフォームアカウントは利用者が登録時に入力した識別子から移行先で再解決する
- CSV出力では表計算ソフトの数式注入を防ぎ、入力ファイルはWeb公開ディレクトリ外で期限付き一時ファイルとして扱う

### 通知タイミング

| **通知タイミング** | **通知種別** |
| --- | --- |
| 動画投稿 | 動画投稿 |
| 待機所作成 | 配信開始前 |
| 配信開始30分前 | 配信開始前 |
| 配信開始予定時間 | 配信開始前 |
| 配信開始 | 配信中 |
| 配信終了 | 配信終了 |
| 配信・動画のタイトル・開始時刻・サムネイルなど、BOTが表示する情報の変更時 | 変更時点の配信状態に対応する通知種別 |

#### 通知タイミングの判定規則

- 待機所を初めて検知した時点で、現在時刻に該当する最も進んだ通知を1回だけ送信する
- 配信開始30分より前に待機所を検知した場合は待機所作成を通知し、その後30分前と開始予定時間に達した時点でも通知する
- 配信開始30分以内に待機所を初めて検知した場合は、待機所作成を別途通知せず、配信開始30分前として通知する
- 配信開始予定時間を過ぎてから待機所を初めて検知した場合は、待機所作成と配信開始30分前を別途通知せず、配信開始予定時間として通知する
- 通知タイミングに達した場合、またはBOTが表示する情報が変更された場合は、既存の通知投稿を削除してから新しい内容を投稿する
- 配信終了後にタイトルやサムネイルなどが変更されても再通知しない
- 通知種別に複数のWebhook URLが設定されている場合は、空ではないすべてのURLへ送信する
- 次の通知種別に有効なWebhook URLがない場合も既存の通知投稿を削除し、新しい通知は投稿しない

#### Discord Embed

- Webhookの表示名とアイコンには、配信プラットフォーム上の配信者名とアイコンを使用する
- Embedには、配信者名とアイコン、配信者ページへのリンク、動画または配信のタイトル、コンテンツへのリンク、通知内容、日時、サムネイル、配信者の表示色、通知作成日時を表示する
- 日時は「投稿時刻」「予定時刻」「開始時刻」「終了時刻」のいずれかとし、Discordの絶対時刻と相対時刻を併記する
- 通知内容は、動画投稿、待機所作成、配信開始30分前、配信開始予定時間、配信開始、配信終了のタイミングごとに定めた文言を使用する
- 配信予定時刻またはタイトルが変更された場合は変更内容を通知文へ追記し、サムネイルが変更された場合はEmbed画像を更新する
- Embedの色は配信者の表示色を優先し、未設定時はYouTubeとTwitchを赤、TwitCastingを緑とする
- 動画または配信をXへ共有するリンクボタンを付ける

## 設定方法
- 管理者用管理ページをWEBページとして用意する
- 管理画面はローカルのログインID、パスワード、TOTPによる二要素認証を必須とし、メール送信と外部アカウント連携には依存しない
- 管理者権限は全権限と管理者管理を行う`owner`と、通常の設定を管理する`administrator`に分け、最後の有効な`owner`を無効化・削除・降格できないようにする
- 初期`owner`の作成と単独`owner`のパスワード・TOTP復旧はCLIから行い、管理者追加時の一度限りのセットアップURLは画面へ1回だけ表示して設置者が安全に共有する
- セッションはMariaDBで管理し、無操作30分・絶対12時間で失効させ、パスワード・TOTP・権限・有効状態の変更時は既存セッションを失効させる
- 認証イベントと管理画面の変更操作は秘密情報を除外した監査ログへ記録し、既定365日保持する
- ポーリングレートはプラットフォームと配信状態ごとに設定できるようにする
- サーバー性能とCronの制約に合わせて、ジョブごとの処理件数、最大実行時間、再試行、バックオフ、リース時間を設定できるようにする
- API Quotaは利用者ごとに異なるため、APIのQuota区分ごとの割当量、通常処理枠、優先処理用予約枠、警告・危険しきい値を設定できるようにする
- 外部Cronの起動間隔はレンタルサーバー側で設定し、管理画面では変更しない
- 通常の配信通知とは別に、複数のDiscord Webhookを管理者向け運用通知先として設定できるようにする
- 障害種別ごとに重要度、連続失敗回数、継続時間、再通知間隔、復旧通知の有無を安全な範囲で設定できるようにする
- 障害通知先が空でも管理画面への障害記録は行い、通知自体の失敗から再帰的な障害通知を生成しない
- 各Cronジョブの最終正常完了時刻を記録し、独立したヘルスチェックCronと外部監視用エンドポイントから遅延を確認できるようにする
- Discord通知の送信結果は処理終了から30日で削除する
- YouTube Data API由来データは保持のための再取得を行わず、Discord通知削除から14日後と、最終取得から30日以内にバックアップからも消える期限の早い方で削除する
- YouTube APIデータを含むアプリ管理バックアップは初期7日、最大14日とし、ホスティング側の保持期間もAPIデータ削除期限の算定に含める
- 配信者や通知設定などのマスターデータは削除時に即時無効化して30日間論理削除し、秘密情報は猶予期間を設けず削除する

### ポーリング設定の初期値

| 状態 | YouTube | Twitch | TwitCasting |
| --- | ---: | ---: | ---: |
| 配信開始予定まで60分超 | 15分 | 15分 | 15分 |
| 配信開始予定まで60分以内 | 1分 | 5分 | 5分 |
| 配信中 | 1分 | 5分 | 5分 |
| 配信終了後の追跡期間内 | 6時間 | 6時間 | 6時間 |
| APIエラー後 | 5分 | 5分 | 5分 |

- ポーリング間隔は60秒以上604800秒以下とし、0による無効化は行わず有効・無効を別設定で管理する
- 実際の実行間隔は外部Cronの起動間隔より短くならない
- 推奨範囲は、配信開始予定まで60分超が5分から24時間、60分以内と配信中が1分から15分、配信終了後が1時間から24時間、APIエラー後が1分から24時間とする

### API利用予算の初期値

- YouTubeは一般Quota区分の割当量を10000 unit/日、アプリケーション上限を80%、通常処理枠を60%、優先処理用予約枠を20%とする
- YouTubeの警告はアプリケーション上限の70%、危険は90%に到達した時点で表示する
- YouTubeの`videos.list`は1リクエスト1 unit、1リクエスト最大50件として推定し、使用しない`search.list`は予算対象に含めない
- TwitchはAPI応答のレート制限ヘッダーを基準に、通常処理を60%まで、優先処理を80%までとし、20%を残す
- TwitCastingは60リクエスト/60秒を初期割当量とし、通常処理を36リクエスト、優先処理を含む上限を48リクエスト、予約枠を12リクエストとする
- 外部APIがエンドポイント別の制限やリセット時刻を示した場合は、管理画面の設定より外部APIの指示を優先する
- 利用量はアプリケーションが実行したAPI操作から算出する推定値であり、外部コンソールの実使用量と一致しない可能性を表示する

### ジョブ設定の初期値

| ジョブ | 処理件数 | 最大実行時間 | 最大試行回数 | リース時間 |
| --- | --- | --- | --- | --- |
| Webhook後続処理 | 50件 | 45秒 | 8回 | 120秒 |
| 配信ポーリング | 20件 | 45秒 | 5回 | 120秒 |
| Webhook購読更新 | 20件 | 45秒 | 8回 | 120秒 |
| Discord通知・削除 | 20件 | 45秒 | 8回 | 120秒 |
| クリーンアップ | 100件 | 45秒 | 20回 | 120秒 |

- 再試行は初回60秒、倍率2.0、最大3600秒、ジッター20%を初期値とする
- 最大試行回数には初回実行を含める
- 初期値は管理画面から安全な範囲で変更できる

### 通知設定方法
- Webページから登録する
- 動画投稿・配信開始前・配信中・配信終了の通知種別ごとに、異なるDiscordチャンネルを設定できるようにする
- 通知設定1つにつき、通知種別ごとに複数のWebhook URLを持てるようにする
- Webhook URLを空にすることで、その通知種別を送信しない設定にできるようにする
- Webhookは漏洩時などに、容易にローテーション出来るようにする
- Discord Webhook URLはHTTPSの`discord.com/api/webhooks/{webhook_id}/{webhook_token}`形式だけを受け付け、クエリやフラグメントは保存しない
- Discord Webhook URL、API資格情報、アクセストークン、Webhook検証用シークレットは、PHPのSodium拡張によるXChaCha20-Poly1305認証付き暗号で保存する
- 暗号化鍵はリポジトリ・DB・Web公開ディレクトリ・Dockerイメージの外へ配置し、鍵IDを使ってローテーションできるようにする
- 秘密情報は画面、通常CSV、ログ、例外へ平文で出力せず、管理画面では既存値を再表示しない
- 通常の移行は秘密情報を除外したCSVと移行先での再入力、または暗号化済みDBバックアップと別経路で保管した鍵を使用する
- 同じ通知設定・通知種別への同一URLはHMAC-SHA-256の鍵付きフィンガープリントで検知し、重複登録しない
- 異なる通知設定や通知種別では同じURLを使用可能とするが、1つの通知イベントで同じURLが複数経路から選ばれた場合は1回だけ送信する
- フィンガープリント鍵はURLの暗号化鍵と分離してリポジトリ・DB・Dockerイメージの外で管理し、鍵IDを使ってローテーションできるようにする

### 配信者登録
- 配信プラットフォーム上のIDは、ハンドル / Screen IDなど、利用者が容易に取得できる情報を入力→登録時にAPI実行でIDに変換する
- カラーコード
- 配信者は必ず所属区分へ関連付け、事務所に所属しない配信者は「個人勢」区分として管理する

## テーブル設計概要

便宜上内部IDとしているが、これはUUIDなどのランダムな値とするのが好ましいかも

### 通知設定

通知設定ごとに、動画投稿・配信開始前・配信中・配信終了の送信先を管理する

| **通知設定ID** | 表示名 |
| --- | --- |
| 1 | 通知設定1 |
| 2 | 通知設定2 |
| 3 | 通知設定3 |

| **通知設定ID** | **通知種別** | **Webhook URL** |
| --- | --- | --- |
| 1 | 動画投稿 | Webhook URL 1 |
| 1 | 配信開始前 | Webhook URL 2 |
| 1 | 配信中 | Webhook URL 3 |
| 1 | 配信終了 | |
| 2 | 動画投稿 | Webhook URL 4 |
| 2 | 配信開始前 | Webhook URL 5 |
| 2 | 配信中 | Webhook URL 6 |
| 2 | 配信終了 | Webhook URL 7 |

### 配信者情報

事務所名と配信者名は、エンティティ本体と`エンティティID + 言語コード`の名称テーブルへ分離する。初期対応言語はBCP 47の`ja`と`en`とし、各エンティティは名称が必ず存在する既定言語を持つ。

| **事務所ID** | コード | 既定言語 | 個人勢区分 |
| --- | --- | --- | --- |
| 1 | hololive | ja | false |
| 2 | nijisanji | ja | false |
| 3 | specialite | ja | false |
| 4 | independent | ja | true |

| **事務所ID** | 言語コード | 正式名 | 短縮名 |
| --- | --- | --- | --- |
| 1 | ja | ホロライブ | ホロライブ |
| 1 | en | hololive | hololive |
| 2 | ja | にじさんじ | にじさんじ |
| 2 | en | nijisanji | nijisanji |
| 3 | ja | すぺしゃりて | すぺしゃりて |
| 3 | en | Specialite | Specialite |
| 4 | ja | 個人勢 | 個人勢 |
| 4 | en | Independent | Independent |

| **配信者ID** | **事務所用内部ID** | 既定言語 | カラーコード |
| --- | --- | --- | --- |
| 1 | 1 | ja | #71e5ff |
| 2 | 2 | ja | #EB4682 |
| 3 | 2 | ja | #CDA5FF |
| 4 | 3 | ja | #b92e46 |
| 5 | 4 | ja | |

| **配信者ID** | 言語コード | 配信者名 |
| --- | --- | --- |
| 1 | ja | 水宮枢 |
| 1 | en | Mizumiya Su |
| 2 | ja | 倉持めると |
| 2 | en | Kuramochi Meruto |
| 3 | ja | 小清水透 |
| 3 | en | Koshimizu Toru |
| 4 | ja | 領国つかさ |
| 4 | en | Ryogoku Tsukasa |
| 5 | ja | しぐれうい |
| 5 | en | Shigure Ui |

- 既定言語の名称は必須とし、名称がない言語へ既定言語を変更できないようにする
- 名称取得は、要求言語、将来の地域コード対応時は基本言語、エンティティの既定言語の順にフォールバックする
- 配信タイトル、プラットフォーム上のチャンネル名、ハンドル、通知設定名、管理画面や通知の固定文言は名称テーブルで翻訳しない
- 英語名は自動翻訳せず、公式英語名または管理者が指定したローマ字表記を登録する

| **配信者ID** | **YouTube Channel ID** | ハンドル |
| --- | --- | --- |
| 1 | XXXXXXXXXXX | XXX |
| 2 | XXXXXXXXXXX | XXX |
| 3 | XXXXXXXXXXX | XXX |
| 4 | XXXXXXXXXXX | XXX |
| 5 | XXXXXXXXXXX | XXX |

| **配信者ID** | **Twitch ID** | ScreenName |
| --- | --- | --- |
| 3 | XXXXXXXXXXX | XXX |
| 4 | XXXXXXXXXXX | XXX |
| 5 | XXXXXXXXXXX | XXX |

| **配信者ID** | **Twitcast ID** | ScreenId |
| --- | --- | --- |
| 3 | XXXXXXXXXXX | XXX |

配信者ごとに複数の通知設定を関連付けられるようにする

| **配信者ID** | **通知設定ID** |
| --- | --- |
| 3 | XXXXXXXXXXX |
