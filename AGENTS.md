# AGENTS.md

## 目的

この `AGENTS.md` は、StreamNotifyBot リポジトリで AI エージェントが効率よく、安全かつ一貫して作業するための共通ルールを定義します。

このリポジトリ内で作業する際は、最初にこのファイルを確認し、以後の調査、実装、検証、報告に適用してください。

目的は、必要な情報だけを短い経路で取得し、既存設計を尊重した最小限の変更で要求を満たし、必要な検証まで完了することです。

---

## プロジェクト概要

StreamNotifyBot は、配信プラットフォームの Webhook と API ポーリングから配信情報を取得し、Discord Webhook へ通知する PHP アプリケーションです。

実行環境は PHP 8.5.9、データベースは MariaDB 10.5 を使用します。実装では、このバージョンで利用できる言語機能、SQL、データ型に限定してください。

現時点の要求と構想は `やりたいこと.md` を参照してください。ただし、同ファイルに記載された未決事項や例示を、確定済みの仕様として扱わないでください。

主な想定機能は次のとおりです。

- 配信プラットフォームへの Webhook 登録と更新
- 配信情報のポーリング
- 配信予約、予約変更、配信開始、配信終了の Discord 通知
- 配信終了後の低頻度ポーリングと通知の削除
- 管理者向け Web 管理画面
- 通知先、配信者、プラットフォーム情報の管理
- テーブル単位の CSV インポートおよびエクスポート
- Docker による実行環境の提供

---

## 基本方針

- ユーザー向けの回答、報告、確認結果、要約、説明は、明示的な指定がない限り日本語で行うこと
- コード、識別子、API 名、CLI コマンド、エラーメッセージなどは、正確性を優先して原文を維持してよい
- 既存の設計、命名規則、ディレクトリ構成、実装パターンを優先すること
- 単純な作業では過剰な計画を行わず、直接実行して検証すること
- 複雑、高リスク、不確実な作業では、必要な範囲だけ調査してから実装すること
- 不明点は推測で埋めず、確認可能な証拠がある場合は調査してから判断すること
- 要求を満たす最小の一貫した変更を行うこと
- 実装したこと自体を完了条件とせず、関連する検証まで完了して初めて作業完了とすること
- 依頼と無関係な変更、過剰なリファクタリング、不要なコード整形を行わないこと
- 同じ結果が得られる場合は、ツール呼び出し、コンテキスト消費、手戻りの少ない方法を優先すること
- コードや設計をレビューする際は、どこかに問題がある前提で敵対的に検証すること
- コマンドの実行確認をユーザーに求める際は、コマンドの意味と実行する理由を日本語で分かりやすく説明すること
- 特別な指示がない限り、リポジトリの上位または外部ディレクトリを読み取らないこと

---

## 情報の優先順位

実装や構造に関する情報が矛盾する場合は、原則として以下の順序で判断してください。

```text
実際のソースコード / 設定
↓
AGENTS.md
↓
パッケージ / ビルド / テスト / lint / 静的解析の設定
↓
PROJECT_STRUCTURE.md
↓
README.md
↓
やりたいこと.md
↓
その他の補助文書
```

`PROJECT_STRUCTURE.md`、`README.md`、`やりたいこと.md` は探索や理解を高速化するための補助情報であり、実際のコードや設定より優先してはなりません。

---

## タスク分類

作業開始時は、必要に応じて複雑さ、リスク、不確実性、並列化可能性、必要なコンテキスト量を内部的に評価し、必要最小限の実行経路を選択してください。

### Fast Path

次のような低リスクで局所的な作業では、詳細な計画を作らず直接対応してください。

- 誤字や小規模な文言の修正
- 影響範囲が明確な単純修正
- 小規模な設定変更
- 容易に検証できる変更

```text
関連箇所を確認
↓
変更
↓
検証
```

### Deep Path

次のような作業では、実装前に必要な構造と依存関係を確認してください。

- 複数ファイルにまたがる変更
- アーキテクチャ、データベース、認証に影響する変更
- 外部 API や公開インターフェースの変更
- Webhook、ポーリング、通知状態のデータフロー変更
- 既存動作との互換性確認が必要な変更

```text
構造確認
↓
依存関係確認
↓
変更範囲決定
↓
実装
↓
検証
↓
回帰リスク確認
```

### Investigation Path

原因が不明な不具合や障害では、推測による修正より先に証拠を集めてください。

```text
証拠収集
↓
仮説作成
↓
仮説検証
↓
根本原因特定
↓
修正
↓
回帰確認
```

---

## コンテキスト取得

- リポジトリ全体を最初から読み込まないこと
- 現在のタスクに必要な最小限の情報から確認を開始すること
- 大きなファイルやディレクトリを「念のため」読み込まないこと
- ファイル検索、シンボル検索、直接依存の追跡を優先すること
- 現在の証拠で判断できない場合にだけ、段階的に調査範囲を広げること
- 既に取得済みで状態が変化していない情報を、不必要に再取得しないこと
- 再チェックを依頼された場合は、古い結果を使い回さず現在の状態を再取得すること
- 作業中にファイル、ブランチ、依存関係、外部状態が変化した可能性がある場合は再取得すること

### 推奨探索順序

```text
AGENTS.md
↓
PROJECT_STRUCTURE.md（存在する場合）
↓
README.md
↓
パッケージ / ビルド / テスト / lint / 静的解析の設定
↓
関連シンボル検索
↓
対象コード
↓
直接依存
↓
関連テスト
```

すべての段階を毎回実行する必要はありません。単純なタスクでは、必要な段階だけを使用してください。

---

## PROJECT_STRUCTURE.md

`PROJECT_STRUCTURE.md` が存在する場合は、リポジトリ探索の索引として使用してください。

次のような場合は作成または更新を検討してください。

- ディレクトリ数が増えて探索コストが高い
- 主要コンポーネントが複数存在する
- エントリポイントやデータフローが分かりにくい
- 同じ構造調査を繰り返している
- 主要コマンドや生成物の場所が非自明

記載対象は、リポジトリの目的、主要ディレクトリと責務、エントリポイント、主要コンポーネント、データフロー、内部依存関係、検証コマンド、生成物、直接編集禁止対象など、探索に有用な情報に限定してください。

通常のロジック修正、文言修正、スタイル調整、コメント修正など、構造に影響しない変更では原則として更新しないでください。記載と実際の構造に差異がある場合は、実際のリポジトリを正とします。

---

## コーディング規約

プロジェクト固有の規約、`.editorconfig`、フォーマッター、静的解析設定が存在する場合は、それらを優先してください。

固有の規約がまだ存在しない場合は、次を既定とします。

- PHP コードは PSR-12 に準拠すること
- 新規 PHP ファイルでは `declare(strict_types=1);` を使用すること
- クラス名、インターフェース名、列挙型名は PascalCase とすること
- メソッド名と変数名は camelCase とすること
- 定数名は UPPER_SNAKE_CASE とすること
- 名前空間とファイル配置は PSR-4 に準拠させること
- 型宣言、戻り値型、読み取り専用プロパティなど、対象 PHP バージョンで利用可能な型安全性を活用すること
- `mixed`、弱い比較、暗黙的な型変換は、外部入力境界を除いて可能な限り避けること
- 外部入力をドメインロジックへ渡す前に、検証と正規化を行うこと
- 日時はタイムゾーンを明示し、保存形式と表示形式を分離すること
- SQL は文字列連結で組み立てず、プレースホルダーまたは既存のクエリ機構を使用すること
- 新規依存関係は、標準 API や既存依存関係で実現できない場合だけ追加すること
- 生成済みファイルを直接編集せず、生成元を変更すること

フレームワークやライブラリを導入した場合は、その標準的な構成と既存プロジェクト内の慣例を優先してください。

---

## セキュリティと秘密情報

このプロジェクトでは Discord Webhook URL、配信プラットフォームの資格情報、管理画面の認証情報を秘密情報として扱います。

- 秘密情報をソースコード、テストデータ、ログ、Docker イメージ、README、コミットへ含めないこと
- `.env` などの実値ファイルをコミットせず、必要に応じてプレースホルダーだけを記載したサンプルを用意すること
- ログや例外へ Webhook URL、API キー、アクセストークン、Cookie、認証ヘッダーを出力しないこと
- Webhook URL は表示時にマスクし、ローテーション可能な設計を維持すること
- 外部から受信する Webhook は、プラットフォームが提供する署名検証や検証トークンを利用すること
- 管理画面、CSV、Webhook、API のすべての入力を信頼せず、認証、認可、CSRF、入力検証を考慮すること
- HTML へ出力する値はコンテキストに応じてエスケープし、XSS を防止すること
- リダイレクト先、URL、ファイル名、CSV セルを外部入力から扱う場合は、それぞれの注入リスクを検討すること
- 秘密情報が差分や出力へ混入した疑いがある場合は、作業を止めてユーザーへ報告すること

---

## 外部 API、Webhook、ポーリング

- 外部 API のレート制限、タイムアウト、リトライ方針を明示すること
- リトライには上限とバックオフを設け、無限再試行を行わないこと
- `429`、一時的な `5xx`、認証失敗、恒久的な入力エラーを区別すること
- 同一イベントの再送や並行実行を想定し、Webhook 受信と Discord 通知を可能な限り冪等にすること
- Webhook とポーリングで同一状態を取得した場合に、通知が重複しないようにすること
- プラットフォーム固有のモデルを、共通の配信状態モデルと安易に同一視しないこと
- 配信予約、開始、終了、削除などの状態遷移は、順不同、欠落、遅延を考慮すること
- 外部 API のレスポンス全体を無条件にログへ出力しないこと
- 時刻比較にはプラットフォームのタイムゾーン、サーバーのタイムゾーン、夏時間の差異を考慮すること
- HTTP クライアントや時計は、テストで代替可能な境界に置くこと

---

## データベースと CSV

- テーブル設計は `やりたいこと.md` の例を出発点とし、確定済みスキーマとは見なさないこと
- 内部 ID、外部プラットフォーム ID、利用者が入力するハンドルや Screen ID を区別すること
- 外部 ID を数値と仮定せず、仕様に適した文字列型として扱うこと
- URL、表示名、カラーコード、言語コードなどに適切な制約を設けること
- スキーマ変更はマイグレーションとして管理し、適用順序とロールバック可能性を検討すること
- トランザクションが必要な複数更新では、途中失敗による不整合を防止すること
- CSV インポートは文字コード、改行、区切り文字、ヘッダー、重複、空値、不正行、大容量入力を考慮すること
- CSV インポートは検証結果を利用者へ提示し、部分更新の可否を明確にすること
- CSV エクスポートでは、表計算ソフトによる数式注入を考慮すること
- テストでは実在人物の秘密情報や実際の Webhook URL を使用しないこと

---

## 実行ルール

- 変更前に対象ファイルの役割を理解すること
- 必要に応じて直接の呼び出し元、依存先、関連テストを確認すること
- 要求を満たす最小の一貫した変更を行うこと
- 依頼と無関係なリファクタリング、コード整形、ファイル移動を行わないこと
- 既存の公開インターフェースや互換性は、変更要求がない限り維持すること
- 大規模変更では、可能な限り機械的変更と挙動変更を分離すること
- 実行可能な作業を説明だけで終わらせず、必要な操作と検証まで完了すること
- 既存のパッケージマネージャーとロックファイルを維持すること
- Docker 構成を変更する場合は、ビルドコンテキスト、秘密情報、実行ユーザー、永続データ、ヘルスチェックへの影響を確認すること

---

## Git運用

- 開発作業はGitHub Flowに従うこと
- デフォルトブランチへ直接変更を加えず、デフォルトブランチを起点に目的ごとの作業ブランチを作成し、実装と確認を行ったうえでPull Requestを経由して統合すること
- ブランチ名は `feature/`、`fix/`、`docs/`、`refactor/`、`chore/` など、変更目的を示す接頭辞を使用すること
- コミットメッセージはConventional Commits形式とし、説明部分は日本語で記述することを基本とすること
- 依頼と無関係な変更を同じブランチやPull Requestに含めないこと
- コミットは依頼された作業の範囲内で実施してよい
- push、Pull Requestの作成・更新、レビュー対応、チェック確認など、Pull Requestに直接関係する作業は包括的に許可されているものとし、個別の確認を求めず実行してよい
- Gitコミットの作成者名とメールアドレスは、実行環境に設定済みの既定値を使用し、リポジトリ側で上書きしないこと

### Pull Request運用

- Pull Requestは、レビュー可能な単一の目的に絞り、別の目的の変更を後から追加しないこと
- 作業中のPull Requestとは独立してマージできる変更は、デフォルトブランチから別ブランチを作成し、別のPull Requestにすること
- 作業中のPull Requestの変更を前提とする後続作業は、親Pull Requestのブランチから子ブランチを作成し、Stacked Pull Requestとして扱うこと
- Stacked Pull Requestでは、子Pull Requestのベースブランチを直前の親Pull Requestのブランチに設定し、変更差分がその段階固有の内容だけになるようにすること
- Pull Request本文には、変更内容、確認方法、影響範囲に加え、親または子Pull Requestがある場合はその前後関係を明記すること
- GitHubのPull request stacks機能が利用可能な場合は、その機能または `gh stack` を使用して前後関係を登録すること。利用できない場合も、ベースブランチによるStacked Pull Request構成を維持すること
- 親Pull Requestがマージされたら、子ブランチを更新されたデフォルトブランチへrebaseし、子Pull Requestのベースブランチをデフォルトブランチへ変更すること
- rebaseやforce pushなど公開済み履歴を書き換える操作は、対象と影響を説明し、ユーザーの明示的な許可を得てから実行すること
- Pull Requestのマージは常にユーザーが手動で行うものとし、エージェントは実行しないこと

---

## 並列作業

- 本当に独立している作業だけを並列化すること
- 前段の結果がないと後段を判断できない処理は並列化しないこと
- 並列化によって実行時間、調査重複、コンテキスト競合が減る場合に使用すること
- 独立した調査、テスト、レビューなどは必要に応じて並列化してよい
- サブタスクの報告をそのまま信用せず、統合時に最終差分と検証結果を確認すること

---

## 検証

実装したこと自体を完了条件にしないでください。変更後は、プロジェクトで利用可能な検証を必要な範囲で実行してください。

候補は次のとおりです。

- 関連テスト
- コードフォーマットまたは lint
- 静的解析
- ビルド
- 設定検証
- Docker イメージのビルド
- 実行時確認
- 最終差分確認

### コード変更

- 最終差分を確認すること
- 直接関連するテストが存在する場合は実行すること
- 通常の検証経路に lint、静的解析、ビルドが含まれる場合は実行すること

### 不具合修正

- 可能な限り不具合を再現するか、発生メカニズムを特定すること
- 修正がその発生メカニズムを解消していることを確認すること
- 実用的な範囲で回帰テストを追加すること

### 設定変更

- 構文を検証すること
- 可能な場合は、その設定を利用する実際のツールや処理で確認すること

### 完了基準

関連する範囲で以下を満たしてください。

```text
Acceptance Criteria: PASS
Relevant Tests: PASS
Lint: PASS or N/A
Static Analysis: PASS or N/A
Build: PASS or N/A
Regression Risk: checked
```

実行できなかった確認項目や残っているエラーは、作業完了時に具体的に報告してください。必要な検証を実施していない場合は、成功と断定しないでください。

---

## 敵対的レビュー

コードや設計をレビューする際は、どこかに問題がある前提で、特に次を検証してください。

- 要件の取りこぼし
- 意図しない挙動変更
- 境界値と例外系
- 状態遷移の競合、重複、順序逆転
- データ形式や時刻の不一致
- 古い参照と隠れた依存関係
- 不十分な入力検証
- テスト不足
- 秘密情報の漏洩
- 認証、認可、CSRF、XSS、SQL インジェクション、SSRF
- Webhook の偽造とリプレイ
- API レート制限違反
- CSV インジェクション
- 無効な前提と未検証の推測

高リスクまたは広範囲の変更では、可能な限り実装とは独立した観点でレビューしてください。

---

## 改善案のIssue管理

- Issueおよびラベルの検索、作成、編集、コメント、クローズは、ユーザーから明示的に依頼された場合だけ実行すること
- 通常作業で具体的な改善案を発見した場合は作業完了時に報告するだけに留め、明示的な依頼なしに既存Issueの検索やIssue操作を行わないこと
- Issue作成を依頼された場合は、対象リポジトリの既存Issueを検索し、重複がなければ改善案1件につきGitHub Issueを1件作成すること
- Issue本文には、背景、問題、改善案、完了条件、関連Pull Requestを記載すること
- 不具合には `bug`、機能、性能、保守性などの改善には原則として `enhancement` ラベルを付けること
- 作成したIssueの採用、保留、クローズはユーザーが判断するものとし、明示的に依頼されない限りIssueをクローズしないこと
- 改善案はプロジェクト内の `改善案.md` では管理せず、GitHub Issueを正とすること
- `StreamNotifyBot` のIssueおよびラベルの作成・編集・コメント・クローズには、現在のユーザープロファイルを基準とする `$env:USERPROFILE\.codex\github-apps\Invoke-StreamNotifyBotIssueBot.ps1` を使用し、GitHub App名義で操作すること
- 複数のIssueまたはラベルをまとめて更新する場合は、対象Issue、変更前後の値、判断理由を事前に表で提示すること
- ユーザーが一括更新を指示または承認した後は、対象ごとの確認を挟まず、可能な限り単一のコマンドまたはツール呼び出しで一括処理すること
- 一括更新が中断、タイムアウト、または一部失敗した場合は、現在の状態を再取得し、未反映の対象だけを一括処理すること
- 一括更新後は対象全体の状態を再取得し、意図した値になっていることを検証して結果を表または簡潔な一覧で報告すること
- Pull Requestの作成・編集・確認とGit操作にはGitHub Appを使用せず、従来のユーザー認証を使用すること
- GitHub上のPull RequestおよびIssueの読み取り、一覧取得、状態確認も、Issue管理に関してはユーザーから明示的に依頼された場合だけ実行すること
- GitHub Appの秘密鍵、JWT、インストールトークンをリポジトリ、ログ、回答へ出力しないこと

---

## 失敗時の処理

- 同じ失敗した方法を、仮説や入力を変えずに繰り返さないこと
- コマンド、前提、実装が失敗した場合は、原因を特定してから次の試行を行うこと
- 失敗によって否定された仮説や前提を更新すること
- 再検証は失敗に最も近い最小単位から開始すること
- 一時的な回避策より、可能な範囲で根本原因の修正を優先すること

失敗時は、誤っていた前提、その前提を否定した証拠、事前に確認すべきだった情報、今後再利用できる知識を整理してください。

---

## 再利用と自動化

- 同じ複数手順の調査、検証、変換を繰り返す場合は、再利用可能なスクリプト、CLI、Skill、文書化された手順への変換を検討すること
- 一度しか使わない単純処理を過剰に自動化しないこと
- ツール呼び出し数、コンテキスト消費、作業時間、ミス発生率、検証の再現性のいずれかを改善する場合に自動化を優先すること
- 自動化によって工程や保守負担が増えるだけの場合は導入しないこと

---

## 永続化する知識

将来の作業でも利用できる、正確で安定した非自明な情報だけを永続化してください。

保存対象の例:

- 非自明なテストやビルドのコマンド
- 既知の失敗しやすい操作
- 重要な設計上の制約
- 主要なエントリポイント
- 再利用可能な調査手順

保存しないもの:

- 一時的なデバッグ状態
- 未確認の推測
- 一度しか使わない値
- すぐ変化する実行結果
- 容易にコードから取得できる大量の情報

実装ルールや制約は `AGENTS.md` に記載し、構造情報は `PROJECT_STRUCTURE.md` に分離してください。

---

## デフォルト実行アルゴリズム

```text
1. AGENTS.md を読む
2. タスクを分類する
3. 必要な場合だけ PROJECT_STRUCTURE.md を読む
4. 完了条件を特定する
5. 必要最小限の関連コードを調査する
6. 最小の一貫した変更を行う
7. 関連する検証を実行する
8. 最終差分と回帰リスクを確認する
9. 構造が変わった場合だけ PROJECT_STRUCTURE.md の更新要否を確認する
10. 再利用価値のある反復作業があれば自動化を検討する
```

---

## 判断ルール

```text
単純なら:
    直接実行する

複雑なら:
    必要な範囲だけ分解する

不確実なら:
    修正前に証拠を集める

構造把握が必要なら:
    PROJECT_STRUCTURE.md を使う

独立しているなら:
    有益な場合だけ並列化する

繰り返すなら:
    再利用・自動化を検討する

失敗したら:
    原因を学んでから再試行する

実装したら:
    検証する

構造が変わったら:
    PROJECT_STRUCTURE.md の更新要否を確認する

関係なければ:
    読まない・変更しない

工程が価値を増やさないなら:
    省く
```
