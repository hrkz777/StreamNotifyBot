# StreamNotifyBot 要件定義書

## 1. 文書の目的

本書は、`やりたいこと.md` に記載された構想を、実装と受け入れ確認に利用できる要件として整理するものです。

元の構想だけでは判断できない事項は「未決事項」、実現方法の提案は「設計案」として区別します。本書の記載によって、未決事項が自動的に確定したものとは扱いません。

## 2. システムの目的

StreamNotifyBot は、複数の配信プラットフォームから配信予定と配信状態を収集し、指定された Discord チャンネルへ通知するシステムです。

次の運用を一元化します。

- 配信者と配信プラットフォーム上のアカウントの登録
- 配信プラットフォームへの Webhook 登録と更新
- Webhook と定期ポーリングを組み合わせた配信状態の追跡
- Discord Webhook を利用した配信通知
- 管理画面による設定
- CSV によるデータの移行とバックアップ

## 3. 前提条件

- アプリケーションは PHP 8.5.9 で実装する
- データベースは MariaDB 10.5 を使用する
- 無料レンタルサーバーなど、常駐プロセスを利用できない環境でも動作可能な構成を目指す
- Dockerfile を用意し、Docker 環境でも実行可能にする
- Discord への送信には Discord Webhook を使用する
- 配信情報の取得方式は、プラットフォームごとに Webhook と API ポーリングを組み合わせる
- 初期対応プラットフォームは YouTube、Twitch、TwitCasting とする

## 4. 利用者と外部システム

| 区分 | 役割 |
| --- | --- |
| 管理者 | 管理画面から通知先、配信者、ポーリング設定などを管理する |
| スケジューラー | Webhook の更新、配信情報のポーリング、通知削除などの定期処理を起動する |
| 配信プラットフォーム | Webhook イベントと配信情報 API を提供する |
| Discord | Webhook 経由で通知を受信し、投稿を保持する |

一般利用者向けのログイン機能や閲覧画面は、現時点の対象に含めません。

## 5. 機能要件

### 5.1 管理画面

| ID | 要件 |
| --- | --- |
| ADM-001 | 管理者は Web ブラウザーから管理画面を利用できること |
| ADM-002 | 管理画面は認証されていない利用者からアクセスできないこと |
| ADM-003 | 管理者は通知設定と通知種別ごとの Discord Webhook URL を登録、参照、変更できること |
| ADM-004 | 管理者は配信者を登録、参照、変更できること |
| ADM-005 | 管理者はプラットフォームと配信状態ごとのポーリング間隔を設定できること |
| ADM-006 | 管理者はジョブ種別ごとの処理件数、最大実行時間、最大再試行回数、バックオフ、リース時間を設定できること |
| ADM-007 | 管理者はプラットフォームの Quota 区分ごとの割当量、通常処理枠、優先処理用予約枠、警告・危険しきい値を設定し、推定使用量を確認できること |
| ADM-008 | 設定値の保存前に値の整合性を検証し、サーバー停止や API 利用予算超過の危険がある値には警告を表示すること |
| ADM-009 | 管理者は複数の運用通知用 Discord Webhook と、障害種別ごとの重要度・しきい値・再通知間隔・復旧通知を設定できること |
| ADM-010 | 管理者は発生中・復旧済みの障害、失敗回数、通知結果、最終正常完了時刻を確認できること |

認証方式、管理者の追加・削除、パスワード回復、監査ログの要否は未決事項です。

### 5.2 通知設定

| ID | 要件 |
| --- | --- |
| CHN-001 | 1 つの論理的な通知設定を表示名で管理できること |
| CHN-002 | 通知設定ごとに、動画投稿、配信開始前、配信中、配信終了の4種類の通知先を独立して設定できること |
| CHN-003 | 通知設定の各通知種別に、複数の Discord Webhook URL を登録できること |
| CHN-004 | Webhook URL を空にすることで、その送信先への通知を無効にできること |
| CHN-005 | 登録済み Webhook URL を新しい URL に置き換え、他の通知設定へ影響を与えずローテーションできること |
| CHN-006 | 無効な Webhook URL が存在しても、他の有効な送信先への通知を継続できること |
| CHN-007 | 1人の配信者に複数の通知設定を関連付けられること |
| CHN-008 | 通知時は、対象となる通知種別に設定された空ではないすべての Webhook URL へ送信すること |
| CHN-009 | Discord Webhook URLはHTTPSの`discord.com/api/webhooks/{webhook_id}/{webhook_token}`形式だけを受け付け、ユーザー情報、明示ポート、クエリ、フラグメント、不正なエンコード、余分なパスを拒否すること |
| CHN-010 | 同じ通知設定と通知種別への同一Webhook URLを、平文検索せずHMAC-SHA-256の鍵付きフィンガープリントで検知すること |
| CHN-011 | 無効化済みの同一Webhook URLが存在する場合は重複登録せず、既存設定の再有効化を案内すること |
| CHN-012 | 異なる通知設定または通知種別では同じWebhook URLを使用できること |
| CHN-013 | 1つの通知イベントで同じWebhook URLが複数の通知設定から選択された場合は、同じURLへ1回だけ送信すること |
| CHN-014 | フィンガープリント鍵をURL暗号化鍵と分離し、複数の鍵IDを使って無停止でローテーションできること |

### 5.3 配信者とプラットフォームアカウント

| ID | 要件 |
| --- | --- |
| STR-001 | 配信者と所属区分について、初期対応言語`ja`・`en`の名称を言語別に管理できること |
| STR-002 | 1 人の配信者に複数プラットフォームのアカウントを関連付けられること |
| STR-003 | 管理者はハンドルや Screen ID など、取得しやすい識別子を入力してアカウントを登録できること |
| STR-004 | 登録時にプラットフォーム API を呼び出し、入力された識別子を安定したプラットフォーム ID へ解決すること |
| STR-005 | 識別子を解決できない場合、登録を確定せず理由を表示すること |
| STR-006 | 配信者の表示色は、有効なカラーコードだけを受け付けること |
| STR-007 | 配信者と所属区分は、対応言語のうち名称レコードが存在する1つの既定言語を持つこと |
| STR-008 | 名称取得時は要求言語、将来地域コードへ対応した場合は基本言語、エンティティの既定言語の順にフォールバックすること |
| STR-009 | 既定言語の名称を直接削除できず、既定言語の変更時は変更先名称の存在を同じトランザクションで検証すること |
| STR-010 | 言語コードはBCP 47形式へ正規化し、コードで定義された対応言語以外を拒否すること |
| STR-011 | プラットフォームAPIから取得した名称と管理者が登録した言語別名称を分離し、API更新で言語別名称を上書きしないこと |
| STR-012 | 事務所短縮名がない場合は同じ言語レコードの正式名を使用し、別言語の短縮名を使用しないこと |

### 5.4 Webhook 購読

| ID | 要件 |
| --- | --- |
| SUB-001 | プラットフォームアカウントの新規登録時、対応プラットフォームが提供する Webhook を購読すること |
| SUB-002 | Webhook 購読に有効期限がある場合、期限切れ前に定期処理で更新すること |
| SUB-003 | 購読状態、有効期限、直近の成功または失敗を確認できること |
| SUB-004 | プラットフォームからの Webhook 検証要求に応答できること |
| SUB-005 | 対応方式が提供されている場合、受信イベントの送信元と完全性を検証すること |
| SUB-006 | 同じイベントが複数回届いても、配信や通知を重複登録しないこと |

### 5.5 配信情報の取得

| ID | 要件 |
| --- | --- |
| POL-001 | Webhook で検知した配信を登録し、API ポーリングの対象にできること |
| POL-002 | 動画投稿、待機所、配信開始、配信終了、およびBOTが表示する情報の変更を識別できること |
| POL-003 | プラットフォーム単位で設定された間隔に従ってポーリングすること |
| POL-004 | 配信状態に応じてポーリング頻度を変更できること |
| POL-005 | 配信終了の起点から正確に168時間未満は低頻度で配信情報を更新すること |
| POL-006 | 外部 API のレート制限と一時障害を考慮し、他の対象へ影響を広げないこと |
| POL-007 | Webhook とポーリングから同じ更新を取得しても、状態を一貫して処理すること |
| POL-008 | Webhook は変更の迅速な検知に使用し、Webhook 受信後に API から取得した最新情報に基づいて状態と通知内容を確定すること |
| POL-009 | API 取得に失敗した場合は不完全な情報で通知せず、上限付きで再試行すること |
| POL-010 | Webhook の欠落、遅延、順序逆転を定期ポーリングで補完すること |
| POL-011 | 古い Webhook を後から受信しても、保存済みの配信状態を過去の状態へ戻さないこと |
| POL-012 | プラットフォームが提供しないイベント種別は、共通機能へ無理に変換せず非対応として扱うこと |
| POL-013 | プラットフォームごとに設定した API 利用予算と予約枠を考慮し、予算超過が見込まれる処理を延期できること |
| POL-014 | API 利用量はアプリケーションが把握できる呼び出しと操作別コストから推定し、外部で消費された利用量と一致しない可能性を管理画面に表示すること |
| POL-015 | API 利用予算が不足する場合は、初回同期や低頻度追跡より、配信開始前と配信中の状態取得を優先できること |
| POL-016 | YouTube アカウントの初回登録時と障害復旧時は、チャンネルの Atom フィードから同期対象の動画 ID を取得すること |
| POL-017 | Atom フィードから取得した動画 ID を重複排除し、最大50件ごとに `videos.list` で最新情報を取得すること |
| POL-018 | `videos.list` の一部バッチが失敗した場合は、成功済みバッチを重複処理せず、失敗したバッチだけを再試行できること |
| POL-019 | YouTube の初回・復旧同期では、`search.list` やアップロード再生リストによる遡及取得を行わず、Atom フィードに含まれる範囲を同期対象とすること |
| POL-020 | ポーリング間隔は60秒以上604800秒以下とし、無効化は間隔0ではなく有効状態で管理すること |
| POL-021 | 実際のポーリング実行間隔は外部Cronの起動間隔より短くならないこと |
| POL-022 | API利用予算と使用量は、プラットフォーム内で制限規則が異なるQuota区分ごとに管理すること |
| POL-023 | 外部APIが返すレート制限値、リセット時刻、エンドポイント固有制限を、管理画面で設定した予算より優先すること |
| POL-024 | 配信終了の起点は、プラットフォームAPIの正式な終了時刻、検証済みWebhookのイベント発生時刻、ポーリングで終了を初めて確認した時刻の順に優先すること |
| POL-025 | 期限前により信頼度の高い終了時刻を取得した場合は、終了時刻と追跡期限をUTCで再計算すること |
| POL-026 | 終了判定後に配信中へ戻った場合は終了時刻と追跡期限を解除し、配信中の追跡を再開すること |
| POL-027 | 現在時刻が追跡期限以上の場合はポーリングせず、クリーンアップ対象にすること |

初期対応プラットフォームの取得分担は次のとおりとします。

| プラットフォーム | Webhook の担当 | API ポーリングの担当 | 初期対象外 |
| --- | --- | --- | --- |
| YouTube | WebSub による動画・待機所の発見とタイトル・説明変更の検知 | 動画種別、予定時刻、開始、終了、タイトル、サムネイルなどの最新状態取得と欠落補完 | なし |
| Twitch | EventSub の `stream.online`、`stream.offline`、`channel.update` による変更検知 | 配信予定、配信状態、タイトル、サムネイルなどの最新状態取得と欠落補完 | 動画投稿通知 |
| TwitCasting | `livestart`、`liveend`、`liveschedulecreate`、`livescheduleupdate`、`livescheduledelete` による変更検知 | 現在の配信、配信予定、タイトル、サムネイルなどの最新状態取得と欠落補完 | 動画投稿通知 |

### 5.6 Discord 通知

| ID | 要件 |
| --- | --- |
| NTF-001 | 動画投稿を検知したとき、「動画投稿」の通知種別へ通知すること |
| NTF-002 | 配信開始30分より前に待機所を初めて検知したとき、「配信開始前」の通知種別へ待機所作成を通知すること |
| NTF-003 | 配信開始まで30分以内かつ開始予定時刻より前に待機所を初めて検知したとき、待機所作成を別途通知せず、「配信開始前」の通知種別へ配信開始30分前として通知すること |
| NTF-004 | 配信開始予定時刻以降かつ配信開始前に待機所を初めて検知したとき、待機所作成と配信開始30分前を別途通知せず、「配信開始前」の通知種別へ配信開始予定時間として通知すること |
| NTF-005 | 配信開始30分より前から追跡している待機所が開始30分前に達したとき、「配信開始前」の通知種別へ通知すること |
| NTF-006 | 追跡中の待機所が配信開始予定時刻に達したとき、「配信開始前」の通知種別へ通知すること |
| NTF-007 | 配信開始を検知したとき、「配信中」の通知種別へ通知すること |
| NTF-008 | 配信終了を検知したとき、「配信終了」の通知種別へ通知すること |
| NTF-009 | タイトル、開始時刻、サムネイルなどBOTが表示する情報の変更を検知したとき、配信終了後を除き、変更時点の状態に対応する通知種別へ通知すること |
| NTF-010 | 新しい通知を行う前に、その配信に対して現在有効な既存の通知投稿をすべて削除し、削除完了後に新規投稿すること |
| NTF-011 | 遷移先の通知種別に有効な Webhook URL がない場合も既存投稿を削除し、新しい通知は投稿しないこと |
| NTF-012 | 対象の通知種別に複数の有効な Webhook URL がある場合、そのすべてへ通知すること |
| NTF-013 | 同じ通知タイミングまたは同じ情報変更に対する通知を重複して投稿しないこと |
| NTF-014 | 配信終了の起点から168時間に到達したとき、その配信に対応する通知投稿を削除対象にすること |
| NTF-015 | Discord への削除または送信が失敗した場合、失敗内容と再試行可能性を記録すること |
| NTF-016 | 配信終了後にタイトルやサムネイルなどBOTが表示する情報が変更されても再通知しないこと |
| NTF-017 | Webhook の表示名とアイコンには、配信プラットフォーム上の配信者名とアイコンを使用すること |
| NTF-018 | Embed に配信者名とアイコン、配信者ページへのリンク、動画または配信のタイトル、コンテンツへのリンク、通知内容、日時、画像、色、通知作成日時を表示すること |
| NTF-019 | 日時フィールドは通知タイミングに応じて「投稿時刻」「予定時刻」「開始時刻」「終了時刻」を使用し、Discord の絶対時刻と相対時刻を併記すること |
| NTF-020 | 配信予定時刻またはタイトルの変更時は変更内容を通知文へ追記し、サムネイルの変更時は Embed 画像へ反映すること |
| NTF-021 | Embed の色は配信者の表示色を優先し、未設定時は YouTube と Twitch を赤、TwitCasting を緑とすること |
| NTF-022 | 動画または配信を X へ共有するリンクボタンを通知へ付けること |
| NTF-023 | 通知投稿の削除失敗は通知削除ジョブの規則に従って再試行し、再試行によって168時間の期限を延長しないこと |
| NTF-024 | Discordから削除対象の投稿に対してNot Foundが返された場合は、削除済みとして扱うこと |
| NTF-025 | 通知投稿の削除完了後に終了時刻だけが訂正されても、削除済みの投稿を復元しないこと |

### 5.7 CSV インポート・エクスポート

| ID | 要件 |
| --- | --- |
| CSV-001 | 管理対象のテーブルごとに CSV をエクスポートできること |
| CSV-002 | 管理対象のテーブルごとに CSV をインポートできること |
| CSV-003 | インポート前に形式と参照整合性を検証し、不正行と理由を表示すること |
| CSV-004 | エクスポート値による表計算ソフトの数式実行を防止すること |
| CSV-005 | 秘密情報を通常の CSV エクスポートへ平文で含めないこと |
| CSV-006 | 通常の CSV では暗号文、ノンス、鍵 ID、フィンガープリントを含む秘密情報関連列も入出力対象外とすること |
| CSV-007 | 通常CSVの対象を設定データに限定し、実行履歴、内部処理状態、API由来キャッシュを対象外とすること |
| CSV-008 | UTF-8 BOM付き、カンマ区切り、CRLF、二重引用符によるエスケープ、必須ヘッダーでエクスポートすること |
| CSV-009 | インポートではBOM付きまたはBOMなしのUTF-8だけを許可し、文字コードや区切り文字を推測変換しないこと |
| CSV-010 | CSV種別・バージョンごとに列名と順序を固定し、重複列、未知列、必須列不足、順序違いを拒否すること |
| CSV-011 | 全行へ同じ整数の`schema_version`を必須とし、未対応または混在したバージョンを拒否すること |
| CSV-012 | 追加、追加・更新、置換のいずれかを明示的に選択し、追加・更新を既定とすること |
| CSV-013 | 置換で不足する既存データは物理削除せず、参照整合性を検証した上で論理削除または無効化すること |
| CSV-014 | 反映前に追加・更新・変更なし・無効化・エラー・警告件数と外部APIの推定Quotaをプレビューすること |
| CSV-015 | プレビューをファイルハッシュ、CSV種別、更新方式、DB基準状態へ結び付け、状態変化時は再プレビューを要求すること |
| CSV-016 | 1ファイルの反映を全件成功または全件ロールバックとし、初期実装では部分成功を提供しないこと |
| CSV-017 | プラットフォームアカウントは利用者が登録時に入力した登録用識別子を使用し、DB反映前に外部APIで安定IDを全件解決すること |
| CSV-018 | ファイル、行、フィールドのサイズ上限を設け、全件をメモリへ展開せずストリーム処理すること |
| CSV-019 | 一時ファイルをWeb公開ディレクトリ外へ保存し、期限切れ、反映完了、失敗時に削除すること |
| CSV-020 | 文字列値を可逆的にエスケープし、表計算ソフトによる数式注入を防止すること |
| CSV-021 | 外部規約を満たすために必須の保持・クリーンアップ方針を、CSVから無効化または絶対上限超過できないこと |

Webhook URL などの秘密情報は、通常 CSV と移行先での再入力、または暗号化済み DB バックアップと別経路で保管した鍵によって移行します。平文の秘密情報をエクスポートする機能は初期実装では提供しません。

### 5.8 障害通知と監視

| ID | 要件 |
| --- | --- |
| ALT-001 | 通常の配信通知とは独立した複数の Discord Webhook へ運用通知を送信できること |
| ALT-002 | 運用通知先が未設定でも、障害を管理画面と構造化ログへ記録すること |
| ALT-003 | 障害を `critical`、`error`、`warning`、`info` の重要度で管理すること |
| ALT-004 | 鍵不足、復号失敗、DB・マイグレーション異常、外部 API の認証・認可エラー、恒久エラーは最初の検出時に通知対象とすること。ただし通知先の取得・復号に必要なDBまたは鍵が利用不能な場合は、外部ヘルスチェックによる検知対象とすること |
| ALT-005 | 一時障害は、障害種別ごとの連続失敗回数または継続時間がしきい値へ到達した場合に通知対象とすること |
| ALT-006 | 同じ障害種別・対象種別・対象 ID の継続障害を同一事象として集約し、既定6時間の間隔でのみ再通知すること |
| ALT-007 | 実際に障害通知を送信した事象が正常化した場合に復旧通知を送信すること |
| ALT-008 | 同種障害が大量に発生した場合はCron実行単位で集約し、詳細を最大20件まで表示すること |
| ALT-009 | 運用通知の送信失敗から新たな障害通知を再帰的に生成せず、他の運用通知先への送信を継続すること |
| ALT-010 | 各Cronジョブの開始・正常完了を記録し、`max(想定実行間隔の2倍, 10分)`を超える遅延を検出できること |
| ALT-011 | DB接続とCronの最終正常完了を確認でき、秘密情報や内部エラー詳細を返さない外部監視用ヘルスチェックを提供すること |
| ALT-012 | サーバーまたはCron全体の停止はアプリケーション自身では通知できないため、外部死活監視を任意で利用できること |
| ALT-013 | DBや暗号鍵の障害時に、平文の緊急通知先や固定資格情報へフォールバックしないこと |

### 5.9 データ保持と削除

| ID | 要件 |
| --- | --- |
| RET-001 | Discord通知の送信結果と通知イベントは処理終了から30日で削除すること |
| RET-002 | YouTube Data API由来データは保持期限延長を目的として再取得せず、Discord通知削除から14日後を削除目安とすること |
| RET-003 | YouTube API由来データを、最終取得から30暦日以内に稼働DBと保持中のバックアップの双方から削除すること |
| RET-004 | YouTube APIデータのDB削除期限を、通知削除日時、最終取得日時、実効バックアップ保持期間から安全側に算定すること |
| RET-005 | YouTube APIデータを含むアプリ管理バックアップは初期7日、最大14日とし、外部ホスティングの保持期間も設定へ記録すること |
| RET-006 | 通知結果をYouTube API由来メタデータから分離し、APIデータ削除後は内部ID、処理状態、日時、エラーコードなどの処理記録だけを保持すること |
| RET-007 | 配信者、所属区分、プラットフォームアカウント、通知設定は削除時に即時無効化し、初期30日の論理削除期間後に匿名化・物理削除すること |
| RET-008 | Webhook URL、API資格情報、セッションなどの秘密情報は論理削除の猶予期間を設けず、削除操作時に利用可能な値を物理削除すること |
| RET-009 | 処理待ち、再試行待ち、リース中、追跡中、発生中障害など未完了のデータを保持期限だけで削除しないこと |
| RET-010 | 保持期間の短縮前に削除予定件数と対象期間をプレビューし、管理者の明示的な確定後に段階的に削除すること |
| RET-011 | OAuth認可取消または利用者による削除要求の対象データは可能な限り即時、遅くとも7暦日以内に削除すること |
| RET-012 | バックアップ復元時は期限切れAPIデータを利用者へ表示または通常処理せず、先にクリーンアップすること |
| RET-013 | APIデータ削除後も履歴参照が必要な場合は、外部IDを元値と無関係な墓標IDへ置換し、表示情報、URL、画像、API由来ハッシュを消去すること |
| RET-014 | 冪等キー、相関ID、Quota使用量などの内部処理情報へYouTubeの外部IDやメタデータを直接複製しないこと |

## 6. 非機能要件

### 6.1 セキュリティ

- 管理画面の全機能を認証および認可の対象とする
- Discord Webhook URL、API キー、アクセストークン、認証情報を秘密情報として扱う
- 秘密情報をログ、画面、CSV、例外メッセージへ平文で出力しない
- 保存が必要な秘密情報は、PHP の Sodium 拡張による XChaCha20-Poly1305 認証付き暗号で暗号化する
- 暗号化ごとに暗号学的に安全な乱数で新しいノンスを生成し、秘密情報の用途、レコード UUID、鍵 ID を追加認証データへ含める
- 暗号化鍵と重複検知用 HMAC 鍵を分離し、リポジトリ、DB、Web 公開ディレクトリ、Docker イメージの外で管理する
- 有効な鍵の不足、暗号文の改ざん、復号失敗時は平文保存へ切り替えず、影響する秘密情報の利用を停止する
- 秘密情報の一覧・詳細表示では平文を返さず、置換、無効化、削除を明示的に区別する
- SQL インジェクション、XSS、CSRF、SSRF、CSV インジェクションを考慮する
- Webhook 受信では、プラットフォームが提供する署名検証や検証トークンを利用する
- Docker イメージへ秘密情報を含めず、実行時設定として注入する

### 6.2 信頼性

- Webhook の重複配信、順序逆転、遅延を前提に冪等に処理する
- 外部 API と Discord の一時障害には、上限付きの再試行とバックオフを使用する
- 1 件の失敗によって、無関係な配信者や通知先の処理を停止させない
- 定期処理を再実行しても二重通知や不整合を起こさない

### 6.3 可搬性と運用性

- PHP が動作する一般的な Web ホスティング環境を想定する
- 常駐ワーカーを必須とせず、Cron 等から起動できる定期処理を提供する
- Dockerfile から再現可能な実行環境を構築できるようにする
- 環境固有の値は設定として分離する
- 障害調査に必要な処理結果を、秘密情報を除外した構造化ログとして記録する
- 障害通知には重要度、障害種別、日時、マスク済み対象、失敗回数、継続時間、秘密情報を含まないエラーコード、相関 ID を含める

### 6.4 保守性とテスト容易性

- プラットフォーム固有処理を共通処理から分離する
- HTTP 通信、時計、永続化、Discord 送信をテストで置き換え可能にする
- 状態遷移、重複イベント、期限境界、外部 API エラーを自動テストの対象とする

## 7. 業務ルール

| ID | ルール |
| --- | --- |
| BR-001 | 配信者と各プラットフォームアカウントは分離して管理する |
| BR-002 | プラットフォームの安定 ID を主な照合キーとし、変更可能なハンドルは登録補助情報として扱う |
| BR-003 | 配信終了の起点から168時間未満は低頻度の監視対象とする |
| BR-004 | 配信終了の起点から168時間に到達した通知投稿は削除対象とする |
| BR-005 | Discord Webhook URL は個別に有効化、無効化、交換できるものとする |
| BR-006 | 外部イベントと定期処理は、同じ状態変更に対して冪等でなければならない |
| BR-007 | 通知種別は、動画投稿、配信開始前、配信中、配信終了の4種類とする |
| BR-008 | 1人の配信者には複数の通知設定を関連付けられるものとする |
| BR-009 | 初回検知時は現在時刻と配信状態に該当する最も進んだ通知を1回だけ送信する |
| BR-010 | 通知タイミングまたは表示情報の変更ごとに、既存投稿を削除してから新規投稿する |
| BR-011 | 配信終了後の表示情報変更は通知しない |
| BR-012 | 対応プラットフォーム一覧はコードで固定し、追加時は Platform Adapter の実装とアプリケーションのデプロイを必要とする |
| BR-013 | 配信者は必ず所属区分へ関連付け、事務所に所属しない配信者は「個人勢」区分として管理する |
| BR-014 | 定期処理と再試行はレンタルサーバーの Cron から PHP CLI コマンドを起動して実行し、専用キューや常駐ワーカーを必須としない |
| BR-015 | Webhook を変更検知、API を最新状態の取得、定期ポーリングを Webhook 欠落の補完として使用する |
| BR-016 | サーバー性能、Cron 制約、API 利用予算に依存する運用値は固定せず、管理画面から安全な範囲で変更可能にする |
| BR-017 | ジョブ設定には安全側の初期値と絶対上下限を設け、項目間の整合性を満たさない値を保存しない |
| BR-018 | 状態別ポーリング間隔とQuota予算には安全側の初期値と絶対上下限を設け、サーバー性能、Cron制約、外部API制限に合わせて変更可能にする |
| BR-019 | 配信終了後の追跡期限はUTCの終了起点に168時間を加えて算定し、現在時刻が期限以上になった時点でポーリング停止と通知削除の対象にする |
| BR-020 | Discord Webhook URLの登録時重複は通知設定・通知種別・URL単位、送信時重複は通知イベント・URL単位で判定する |
| BR-021 | 初期の多言語管理は事務所名・事務所短縮名・配信者名の`ja`・`en`に限定し、UI文言、通知固定文言、外部サービス由来の名称やタイトルを自動翻訳しない |
| BR-022 | 単発の一時障害は再試行へ任せ、認証失敗、鍵不足、改ざん、恒久エラーは最初の検出時に障害通知する |
| BR-023 | 障害の確認済み操作だけでは復旧扱いにせず、正常処理の確認または対象の無効化によって終了する |
| BR-024 | Discord投稿の削除期限、YouTube API由来データの削除期限、通知処理結果の保持期限を別々に管理する |
| BR-025 | YouTube API由来データの削除期限は、`通知ライフサイクル完了日時 + 14日`と`最終取得日時 + 30日 - 実効バックアップ保持期間`の早い方とする |
| BR-026 | 削除済みの「個人勢」既定区分は作成せず、同区分自体を削除不可とする |

## 8. 対象外

現時点では、次を確定した対象に含めません。

- Discord Bot トークンを利用した Bot アカウント方式
- 一般利用者向けのアカウントと画面
- 配信映像やアーカイブの保存
- 通知以外の Discord コミュニティ管理機能
- 配信プラットフォームへの投稿や配信操作
- 課金、契約、マルチテナント機能

## 9. 未決事項

| ID | 論点 | 決定が必要な理由 |
| --- | --- | --- |
| TBD-002 | 管理画面の認証方式 | ホスティング制約と必要な安全性に影響するため |
| TBD-003 | 使用する PHP フレームワーク | 配置要件、依存関係、保守方法に影響するため |

## 10. 初期受け入れ条件

- 管理者が4種類の通知種別と種別ごとに複数の Discord Webhook URL を持つ通知設定、配信者、プラットフォームアカウントを登録できる
- 1人の配信者へ複数の通知設定を関連付けられる
- 人が入力しやすいプラットフォーム識別子から安定 ID を取得して保存できる
- 事務所名・短縮名・配信者名を`ja`・`en`で管理し、要求言語がない場合も既定言語へフォールバックできる
- Webhook で新規配信を検知し、ポーリングによって動画投稿、待機所、30分前、開始予定時間、開始、終了、表示情報変更を追跡できる
- 各通知タイミングで既存投稿を削除し、通知種別に設定されたすべての有効な送信先へ新規投稿できる
- 通知種別のURLが空の場合は、既存投稿を削除したうえで新規投稿しない
- 同じ通知設定・通知種別へのWebhook URL重複登録を防止し、複数経路から同じURLが選ばれても通知イベントごとに1回だけ送信できる
- 状態別の文言、日時、リンク、画像、色を持つ Discord Embed と X 共有ボタンを表示できる
- 配信終了の起点から168時間未満は低頻度ポーリングを行い、168時間に到達した後のCron実行で通知削除を実行できる
- Webhook 購読に期限がある場合、期限切れ前に更新できる
- 管理対象データを安全に CSV で入出力できる
- レンタルサーバー相当の PHP 環境と Docker 環境の双方で実行手順を再現できる
- 主要な正常系、重複イベント、外部サービス障害、期限境界が自動テストで確認されている

## 11. 関連文書

- [基本設計書](基本設計書.md)
- [データ設計書](データ設計書.md)
- [初期構想メモ](../やりたいこと.md)
